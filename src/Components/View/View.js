import React, { Component } from 'react';
import Triggers from '../Triggers/Triggers'

class View extends Component {
    render() {
        return (
            <div>
                <Triggers />
            </div>
        )
    }
}

export default View;
